# 面向对象

## 类

> 对象的类型 也称作对象的类，描述了对象的内部表示及它支持的方法与操作。

## 对象

> 程序中存储的所有数据都是对象。每个对象都有一个标识、一个类型（也叫作它的类）和一个值。

is 运算符用于比较两个对象的标识。内置函数 `type()` 则返回一个对象的类型。因为可以通过定义类对类型进行特殊化，所以检查类型的更佳方式是用内置函数 `isinstance (object , type)`，因为isinstance() 函数能够辨别继承，所以它是检查所有Python对象类型的首选方式。

### 对象的创建与销毁
